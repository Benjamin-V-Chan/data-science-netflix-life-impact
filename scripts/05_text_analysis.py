# 1. import pandas, pathlib, sklearn.feature_extraction, sklearn.decomposition, textblob
# 2. load cleaned DataFrame
# 3. apply TextBlob to compute sentiment polarity and subjectivity for advice
# 4. add those sentiment columns
# 5. vectorize 'meaningful_advice' via TF-IDF
# 6. apply NMF or LDA to extract topics
# 7. save sentiment-augmented DataFrame and topic components to outputs/
